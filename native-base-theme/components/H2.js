import variable from './../variables/platform';

export default (variables = variable) => {
  const h2Theme = {
    color: "#7f7f7f",
    fontSize: 36,
    letterSpacing: 0.2,
    fontWeight: '600',
  };

  return h2Theme;
};
